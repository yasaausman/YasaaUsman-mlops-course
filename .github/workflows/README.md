# .github/workflows/

Empty on purpose — CI/CD workflow files are added here in the Week 10 lab
(GitHub Actions pipeline that retrains/redeploys on new data). This README is
just a placeholder so the folder exists in git from day one (git doesn't
track empty directories).
