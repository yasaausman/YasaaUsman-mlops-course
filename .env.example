# Copy this file to .env and fill in real values. .env itself is gitignored — never commit it.

# Week 2 — experiment tracking
WANDB_API_KEY=
MLFLOW_TRACKING_URI=

# Add more as later weeks require them (e.g. cloud credentials in Week 11).
